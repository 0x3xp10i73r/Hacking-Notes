// Giscus comments are injected via overrides/partials/comments.html instead
// of here, so they only load on pages that opt in with `comments: true` in
// front matter. This file is kept as a slot in case you want site-wide JS
// later — safe to leave empty.
